import(ggplot2)
importFrom(memisc, percent, spss.system.file, description, measurement)
importFrom(reshape, melt)
importFrom(foreign, read.spss)
importFrom(plyr, ddply)
importFrom(Hmisc, wtd.quantile)

export(
plot.intsvy.mean,
plot.intsvy.table,
plot.intsvy.reg,

intsvy.var.label,
intsvy.select.merge,
intsvy.table,

pirls.ben.pv,
pirls.log,
pirls.log.pv,
pirls.mean.pv,
pirls.mean,
pirls.per.pv,
pirls.reg.pv,
pirls.reg,
pirls.rho.pv,
pirls.rho,
pirls.select.merge,
pirls.table,
pirls.var.label,

piaac.table,
piaac.ben.pv,
piaac.mean.pv,
piaac.mean,
piaac.reg.pv,
piaac.reg,

pisa.ben.pv,
pisa.log,
pisa.log.pv,
pisa.mean.pv,
pisa.mean,
pisa.per.pv,
pisa.reg.pv,
pisa.reg,
pisa.select.merge,
pisa.table,
pisa.var.label,

timss.ben.pv,
timss.log,
timss.log.pv,
timss.mean.pv,
timss.mean,
timss.per.pv,
timss.reg.pv,
timss.reg,
timss.rho.pv,
timss.rho,
timss.table,

timssg4.select.merge,
timssg4.var.label,
timssg8.select.merge,
timssg8.var.label
)

S3method(summary, intsvy.reg)
S3method(print, intsvy.reg)
S3method(plot, intsvy.mean)
S3method(plot, intsvy.reg)
S3method(plot, intsvy.table)
